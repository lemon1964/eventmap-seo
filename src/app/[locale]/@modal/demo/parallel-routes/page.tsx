// src/app/[locale]/@modal/demo/parallel-routes/page.tsx
export default function EmptyParallelRoutesModalSlotRoute() {
  // Clears the @modal slot when navigation returns to the children-only demo.
  return null;
}
